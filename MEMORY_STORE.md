# Memory Store - سجل الذكريات والدروس المستفادة

يحتفظ هذا الملف بالدروس المستفادة، والقرارات التقنية لتفادي تكرار الأخطاء ولتحسين الأداء.

## [lesson] حل مشكلة 401 عند ربط Claude Code مع Kimi API
- **التاريخ:** 2026-08-04
- **الوكلاء المساهمون:** `[agent-optimizer]`, `[code-architect]`
- **المشكلة:** ظهور خطأ فشل المصادقة (401 Invalid Authentication) عند إعطاء مفتاح Kimi لسطر الأوامر `Claude Code CLI`.
- **السبب:** أداة Claude مبرمجة حصرياً للاتصال بخوادم Anthropic (وبصيغتها الخاصة)، بينما Kimi يعتمد صيغة `OpenAI Compatible`.
- **الحل المعتمد:**
  1. تشغيل `LiteLLM` في الخلفية ليعمل كمترجم محلي (Proxy) يحول الطلبات من صيغة Anthropic إلى OpenAI Compatible ويوجهها لـ Moonshot.
  2. توجيه المتغير `ANTHROPIC_BASE_URL` ليقرأ من الخادم المحلي (`http://localhost:4000`).
  3. إنهاء عملية `LiteLLM` آلياً فور خروج المستخدم من أداة Claude.

## [lesson] حل مشكلة تشغيل الصوت التلقائي في ExoPlayer وفقدان AudioFocus
- **التاريخ:** 2026-08-05
- **الوكلاء المساهمون:** `[android-kotlin-pro]`, `[agent-optimizer]`
- **المشكلة:** عند السحب الأفقي (`HorizontalPager`) للانتقال لآية جديدة، يتغير العرض ولكن لا يعمل الصوت تلقائياً، أو ينقطع فوراً بسبب تداخل أوامر التشغيل والـ `Text-To-Speech (TTS)`.
- **السبب:**
  1. استدعاء `controller.stop()` قبل `setMediaItem()` يجعل `ExoPlayer` يفقد التركيز الصوتي (`AudioFocus`) فوراً، وعند طلب التشغيل مجدداً قد لا يستعيده بسرعة، خصوصاً مع النطق المتزامن لـ `TTS`.
  2. مزامنة حالة `HorizontalPager` مع `ViewModel` باستخدام `snapshotFlow` كانت تلتقط حتى التغييرات البرمجية مما يسبب حلقة غير منتهية من إعادة التشغيل.
- **الحل المعتمد:**
  1. **حذف `controller.stop()`:** عند تغيير المسار الصوتي، يكفي استدعاء `setMediaItem(...)` تليها `prepare()` و `play()`. هذه الطريقة تستبدل المقطع بسلاسة دون التخلي القسري عن `AudioFocus`.
  2. **تأخير الـ TTS:** تم فصل قراءة الـ `TTS` (أرقام الآيات) عن زر التشغيل. حيث يتم الإعلان بصوت الـ TTS فقط بعدما يؤكد الـ `ExoPlayer` أنه بدأ التشغيل `onIsPlayingChanged`، مع إضافةหนأخير بسيط (`delay(400)`) لمنع التداخلات.
  3. **عزل التغييرات البرمجية في Compose:** تم استخدام المتغير `isProgrammaticScroll` لتخطي إرسال أحداث التمرير للـ `ViewModel` إذا كان التمرير مبرمجاً (تلقائياً) وليس نتيجة سحب من المستخدم.
