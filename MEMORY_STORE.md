# Memory Store - سجل الذكريات والدروس المستفادة

يحتفظ هذا الملف بالدروس المستفادة، والقرارات التقنية لتفادي تكرار الأخطاء ولتحسين الأداء.

## [lesson] حل مشكلة 401 عند ربط Claude Code مع Kimi API
- **التاريخ:** 2026-08-04
- **الوكلاء المساهمون:** `[agent-optimizer]`, `[code-architect]`
- **المشكلة:** ظهور خطأ فشل المصادقة (401 Invalid Authentication) عند إعطاء مفتاح Kimi لسطر الأوامر `Claude Code CLI`.
- **السبب:** أداة Claude مبرمجة حصرياً للاتصال بخوادم Anthropic (وبصيغتها الخاصة)، بينما Kimi يعتمد صيغة `OpenAI Compatible`.
- **الحل المعتمد:**
  1. تشغيل `LiteLLM` في الخلفية ليعمل كمترجم محلي (Proxy) يحول الطلبات من صيغة Anthropic إلى OpenAI Compatible ويوجهها لـ Moonshot.
  2. توجيه المتغير `ANTHROPIC_BASE_URL` ليقرأ من الخادم المحلي (`http://localhost:4000`).
  3. إنهاء عملية `LiteLLM` آلياً فور خروج المستخدم من أداة Claude.

## [lesson] حل مشكلة تشغيل الصوت التلقائي في ExoPlayer وفقدان AudioFocus
- **التاريخ:** 2026-08-05
- **الوكلاء المساهمون:** `[android-kotlin-pro]`, `[agent-optimizer]`
- **المشكلة:** عند السحب الأفقي (`HorizontalPager`) للانتقال لآية جديدة، يتغير العرض ولكن لا يعمل الصوت تلقائياً، أو ينقطع فوراً بسبب تداخل أوامر التشغيل والـ `Text-To-Speech (TTS)`.
- **السبب:**
  1. استدعاء `controller.stop()` قبل `setMediaItem()` يجعل `ExoPlayer` يفقد التركيز الصوتي (`AudioFocus`) فوراً، وعند طلب التشغيل مجدداً قد لا يستعيده بسرعة، خصوصاً مع النطق المتزامن لـ `TTS`.
  2. مزامنة حالة `HorizontalPager` مع `ViewModel` باستخدام `snapshotFlow` كانت تلتقط حتى التغييرات البرمجية مما يسبب حلقة غير منتهية من إعادة التشغيل.
- **الحل المعتمد:**
  1. **حذف `controller.stop()`:** عند تغيير المسار الصوتي، يكفي استدعاء `setMediaItem(...)` تليها `prepare()` و `play()`. هذه الطريقة تستبدل المقطع بسلاسة دون التخلي القسري عن `AudioFocus`.
  2. **تأخير الـ TTS:** تم فصل قراءة الـ `TTS` (أرقام الآيات) عن زر التشغيل. حيث يتم الإعلان بصوت الـ TTS فقط بعدما يؤكد الـ `ExoPlayer` أنه بدأ التشغيل `onIsPlayingChanged`، مع إضافةหนأخير بسيط (`delay(400)`) لمنع التداخلات.
  3. **عزل التغييرات البرمجية في Compose:** تم استخدام المتغير `isProgrammaticScroll` لتخطي إرسال أحداث التمرير للـ `ViewModel` إذا كان التمرير مبرمجاً (تلقائياً) وليس نتيجة سحب من المستخدم.

## [lesson] حفظ موقع القراءة واسترجاعه تلقائياً للمكفوفين
- **التاريخ:** 2026-08-05
- **الوكلاء المساهمون:** `[android-kotlin-pro]`, `[agent-optimizer]`
- **التحدي:** يحتاج المستخدم الكفيف لإغلاق التطبيق في أي وقت والعودة ليجده عند نفس السورة والآية التي توقف عندها، دون الحاجة للبحث اليدوي في قائمة السور.
- **الحل المعتمد:**
  1. تم تعديل `QuranRepository` ليحتفظ بمعلومات (Surah ID, Ayah Index) داخل `SharedPreferences` بشكل لحظي عند كل استدعاء لدالة `goToAyah` أو `loadSurah`.
  2. تم إدراج كود استرجاع الموقع المحفوظ داخل الدالة التمهيدية `init` في الـ `QuranViewModel`. عند أول إقلاع، يتم قراءة الموضع الأخير وتوجيه المستخدم إليه فوراً (بوضع التشغيل المؤقت autoPlay = false) ليكون التطبيق جاهزاً للاستئناف من حيث انتهى.

## [lesson] تحسين تجربة TalkBack وعزل نطق رقم الآية
- **التاريخ:** 2026-08-05
- **الوكلاء المساهمون:** `[jetpack-compose-ui]`, `[agent-optimizer]`
- **التحدي:** التداخل المزعج أثناء تشغيل الآيات حيث ينطق الـ TalkBack "الآية رقم كذا، ثم النص" كلما انتقل التركيز للكارت، في حين يريد المستخدم الاستماع للتلاوة دون مقاطعة مستمرة برقم الآية إلا عند الطلب.
- **الحل المعتمد:**
  1. **الكارت الرئيسي (`AyahCard`):** تم الاكتفاء بوضع `contentDescription = ayah.textArabic` فقط ليقرأ النص القرآني بسلاسة تامة.
  2. **الكارت الثانوي (`AyahNumberCard`):** تم جعله قابلاً للضغط `clickable` وخصص لنطق رقم الآية بوضوح عبر الـ `SpeechManager` (دالة `announce`) عند الضغط الصريح عليه فقط.
  3. **إلغاء النطق التلقائي المبرمج:** تم إزالة التكديس (`pendingAyahAnnouncement`) الذي كان يسبق كل آية لضمان تلاوة صافية بدون أصوات مركبة مقحمة بين الآيات.

## [decision] توزيع نماذج Kimi وتخصيص مهام Claude Code
- **التاريخ:** 2026-08-06
- **الوكلاء المساهمون:** `[code-architect]`, `[devops-deployer]`, `[persistent-memory-engine]`
- **التحدي:** يقوم Claude Code باستخدام نماذج مختلفة للمهام المعمارية (Opus) والمهام البرمجية اليومية (Sonnet) والمهام الفرعية السريعة (Haiku). كان استخدام نموذج واحد ثابت لكافة المهام يبطئ العمليات السريعة ويزيد التكلفة عند استخدام نموذج `kimi-k3` القوي لكل صغيرة وكبيرة.
- **الحل المعتمد (النمط الهيكلي):**
  1. تحديث سكريبت الإطلاق `kimi.ps1` لتعيين متغيرات البيئة `ANTHROPIC_DEFAULT_*_MODEL` لتوزيع المهام على نماذج Kimi بشكل دقيق.
  2. **المهام المعقدة والبرمجية (Opus/Sonnet):** خُصص لها النموذج القوي `kimi-k3` لضمان أعلى دقة وذكاء في كتابة الكود.
  3. **المهام الفرعية وأدوات الخلفية (Haiku):** خُصص لها النموذج السريع `kimi-k2.7-code-highspeed` لتنفيذ الأوامر الفرعية والاستفسارات بسرعة لحظية.
  4. تم دمج مفاتيح (Parameters) في السكريبت للتبديل السريع بين وضع السرعة القصوى (`-Fast`) ووضع التفكير العميق (`-Opus`)، وتم إنشاء دليل الاستخدام `KIMI_GUIDE.md` كمرجع.

## [lesson] قيود نظام أندرويد على إمكانية الوصول (TalkBack)
- **التاريخ:** 2026-08-06
- **الوكلاء المساهمون:** `[android-kotlin-pro]`, `[agent-optimizer]`
- **التحدي:** أراد المستخدم إيقاف قارئ الشاشة TalkBack برمجياً عند بدء جلسة جديدة لضمان عدم تداخله مع المساعد الصوتي الخاص بالتطبيق.
- **السبب:** يفرض نظام أندرويد قيوداً أمنية صارمة تمنع تطبيقات الطرف الثالث من التحكم بخدمات الوصول (Accessibility Services) وإيقافها برمجياً.
- **الحل المعتمد:** تم استخدام `AccessibilityManager` لاكتشاف حالة الـ TalkBack، وفي حال كان مفعلاً، يقوم التطبيق بتوجيه نداء صوتي حازم للمستخدم يطلب منه إيقافه يدوياً باستخدام الاختصار العالمي (الضغط المطول على زري الصوت معاً لثلاث ثوانٍ).

## [lesson] التحكم في الانتقال التلقائي للآيات في الوضع العادي
- **التاريخ:** 2026-08-06
- **الوكلاء المساهمون:** `[android-kotlin-pro]`
- **التحدي:** كان المشغل ينتقل للآية التالية تلقائياً حتى وإن كان وضع "الاستماع المتواصل" معطلاً.
- **الحل المعتمد:** تم تعديل دالة `onAyahPlaybackEnded` للتأكد من أنه في حال تعطيل الاستماع المتواصل، يتوقف المشغل تماماً (باعتبار `Player.STATE_ENDED`) دون استدعاء `goToAyah`، ليكون الانتقال للآية التالية حصرياً عبر السحب اليدوي من المستخدم.

## [lesson] تسريع اكتشاف انقطاع الإنترنت (Fail-Fast)
- **التاريخ:** 2026-08-06
- **الوكلاء المساهمون:** `[backend-architect]`, `[performance-optimizer]`
- **التحدي:** عند انقطاع الإنترنت، كان التطبيق يظل صامتاً لفترة طويلة (حتى 8-10 ثوانٍ) بانتظار استجابة الخادم قبل إعلان الفشل.
- **الحل المعتمد:** تم تخفيض `connectTimeout` و `readTimeout` في طلبات جلب الروابط من السيرفر داخل `QuranRepository` إلى `4000ms`. هذا أدى إلى تسريع الإعلان عن انقطاع الإنترنت وتفعيل دالة `handleNetworkPlaybackError` بشكل شبه فوري.

## [lesson] فصل المشاريع بشكل آمن باستخدام Git
- **التاريخ:** 2026-08-06
- **الوكلاء المساهمون:** `[devops-deployer]`
- **التحدي:** الحاجة إلى استخراج نسخة قديمة (Commit محدد) من التطبيق ليكون أساساً لتطبيق جديد تماماً، مع الحفاظ على التعديلات الحالية للتطبيق الحالي بأمان.
- **الحل المعتمد:** بدلاً من استخدام أوامر الأرشفة المعقدة، تم الاعتماد على أوامر Git النقية عبر نسخ المجلد بـ `git clone`، ثم تنفيذ `git checkout <commit_hash>` للعودة للنسخة المستهدفة، ثم إزالة المجلد المخفي `.git` نهائياً، وإعادة تهيئة مستودع جديد بـ `git init`. هذه الطريقة تضمن استقلالية تامة للمشروع الجديد ورفعه بأمان إلى مستودع GitHub جديد.

## [decision] اعتماد معيار MCP وتحديث قدرات الذكاء الاصطناعي لفريق الوكلاء
- **التاريخ:** 2026-08-06
- **الوكلاء المساهمون:** `[github-talent-scout]`, `[agent-optimizer]`, `[code-architect]`
- **القرار:** تم اعتماد المعايير والأدوات الحديثة لعام 2026 لترقية قدرات فريق الذكاء الاصطناعي كقواعد عامة للمشروع (`AGENTS.md`):
  1. **تفعيل Model Context Protocol (MCP):** اعتماد خوادم MCP القياسية لتزويد الوكلاء بأدوات تصفح المتصفح (Playwright MCP) وإدارة الملفات والـ APIs ببروتوكول موحد.
  2. **أتمتة الأندرويد بـ Maestro:** اعتماد أداة Maestro كخيار محلي أسرع لوكلاء الأندرويد لاختبار الواجهات والمحاكيات بسكريبتات بسيطة دون تعقيدات الاختبار التقليدي.
  3. **إدراج مهارة `[github-talent-scout]` بصفة دائمة:** للبحث والتقييم التلقائي للمستودعات مفتوحة المصدر الآمنة وعالية الأداء فور ظهور أي فجوة تقنية.

## [lesson] منع إعادة الرسم العشوائي في القوائم (Recomposition Leak)
- **التاريخ:** 2026-08-08
- **الوكلاء المساهمون:** `[performance-optimizer]`, `[agent-optimizer]`
- **التحدي:** كان تغير حالة الشاشة (إضاءة/إطفاء) يتسبب في إعادة رسم (Recomposition) جميع بطاقات الآيات المرئية في `HorizontalPager` بشكل فوري مما يهدر البطارية.
- **السبب:** تمرير المتغيرات سريعة التغير كقيم مباشرة (Primitives) إلى مكونات بداخل القوائم يؤدي إلى تقييمها فوراً عند تغير الـ State الأب.
- **الحل المعتمد:** تحويل القيم المباشرة إلى (Providers/Lambdas) مثل `isScreenOffModeProvider: () -> Boolean` لتأجيل قراءة الحالة إلى وقت التكوين الفعلي داخل العنصر، مما كسر سلسلة إعادة الرسم الجماعي تماماً.

## [lesson] حل تضارب الإيماءات مع قارئ الشاشة TalkBack
- **التاريخ:** 2026-08-08
- **الوكلاء المساهمون:** `[jetpack-compose-ui]`, `[agent-optimizer]`
- **التحدي:** استخدام `detectTapGestures` أو الـ modifiers العادية للنقر المزدوج يتجاوز إيماءات TalkBack أو يعطلها للمكفوفين.
- **الحل المعتمد:** 
  1. الاستغناء عن الإيماءات المباشرة واستخدام `blindAccessibleClickable` الذي يفصل بين تفاعل المبصرين وتفاعل المكفوفين.
  2. استخدام `clearAndSetSemantics` وتضمين الإجراءات المخصصة عبر `customActions = listOf(CustomAccessibilityAction(...))` لإجبار TalkBack على عرض الوظائف بوضوح (مثل النقر المزدوج مع الاستمرار لإعادة التلاوة) بشكل متوافق تماماً مع إيماءات أندرويد القياسية للمكفوفين.

## [lesson] حماية الجلسة المحفوظة من سباق التزامن (State Race Condition)
- **التاريخ:** 2026-08-08
- **الوكلاء المساهمون:** `[android-kotlin-pro]`, `[offline-sync-db]`
- **التحدي:** بالرغم من حفظ الجلسة بنجاح، كان التطبيق يمسحها ويعود للآية الأولى عند التشغيل.
- **السبب:** `HorizontalPager` كان يُبنى ويقرأ قيمة `initialPage` بينما قائمة الآيات لا تزال قيد التحميل من قاعدة البيانات (فارغة)، مما يجبر المكون على التصفير.
- **الحل المعتمد:** تم تغليف مكون الـ Pager بالكامل بشرط التأكد من تحميل البيانات `if (ayahs.isNotEmpty())`. هذا يمنع التهيئة المبكرة ويضمن قراءة موضع الآية المحفوظ بدقة بعد توفر البيانات.

## [lesson] التحقق من توافر ملفات الصوت المجزأة قبل الاعتماد
- **التاريخ:** 2026-08-08
- **الوكلاء المسؤولون:** [code-architect], [agent-optimizer]
- **الحدث:** فشل تشغيل تلاوات القارئ "عبد الرشيد صوفي" بسبب إرجاع خوادم الصوت 403 Forbidden وافتقار الواجهات لملفات صوتية مجزأة (آية بآية).
- **السبب:** بعض القرّاء يتوفرون فقط بنسق "سورة كاملة"، وتطبيقنا مبني هندسياً للعمل بنسق "آية بآية" لضمان تفاعل TalkBack عند نقر المكفوفين على آية معينة.
- **الحل المعتمد:** تم استبعاد القارئ غير المتوافق مع معمارية التطبيق للحفاظ على موثوقية التشغيل، وتم الاعتماد فقط على القرّاء الذين يمتلكون تلاوات مجزأة ومختبرة عبر AlQuranCloud (مثل الحصري، المنشاوي، عبد الباسط).
