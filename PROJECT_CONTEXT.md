# السياق المعماري لمشروع (Blind App - تطبيق القرآن للمكفوفين)

## 📌 نبذة عن المشروع
التطبيق مخصص لخدمة المكفوفين بشكل أساسي عبر تقديم واجهة صوتية وتفاعلية لقراءة وسماع القرآن الكريم. يعتمد التطبيق على التفاعل الدقيق مع قارئ الشاشة (TalkBack) وإيماءات اللمس المخصصة لتعويض الرؤية.

## 🏗️ المعمارية البرمجية (Architecture)
- **النمط المعماري:** Clean Architecture + MVVM (Model-View-ViewModel)
- **واجهة المستخدم (UI Layer):** Jetpack Compose بأسلوب Declarative UI، مع التركيز التام على إمكانية الوصول (Accessibility).
- **إدارة الحالة (State Management):** الاعتماد على `StateFlow` في الـ `ViewModel` كمصدر الحقيقة الوحيد (Single Source of Truth).
- **حقن الاعتماديات (DI):** Dagger Hilt (متوقع أو مستخدم حالياً).
- **تشغيل الصوتيات:** `ExoPlayer` / `Media3` كخدمة في الخلفية (Foreground Service) لضمان استمرارية التشغيل.

## 🛠️ التقنيات والمكتبات الأساسية (Tech Stack)
- **اللغة:** Kotlin
- **واجهة المستخدم:** Jetpack Compose + Material Design 3
- **الشبكة:** Retrofit لجلب البيانات (السور، الآيات، بيانات المقرئين من MP3Quran / AlQuranCloud API).
- **قواعد البيانات:** Room DB (إن وجد للتخزين دون اتصال) و `EncryptedSharedPreferences` لتخزين الجلسات والبيانات الحساسة بأمان.
- **إمكانية الوصول (Accessibility):** استخدام مكثف لـ `clearAndSetSemantics` و `customActions` مع مكون `blindAccessibleClickable` المخصص لدعم TalkBack بشكل مثالي.

## ⚠️ قواعد ومبادئ صارمة (Strict Conventions)
1. **أداء الواجهة (Recomposition):** يُمنع منعاً باتاً تمرير متغيرات الحالة سريعة التغير (مثل `isScreenOffMode`) كقيم مباشرة (Primitives) في قوائم Lazy/Pager؛ يجب تغليفها كـ Providers/Lambdas `() -> T`.
2. **إمكانية الوصول (TalkBack First):** الإيماءات المباشرة مثل `detectTapGestures` لا يجب أن تلغي أو تتداخل مع دلالات TalkBack. يجب استخدام `SemanticsPropertyReceiver.customActions` لتعريف الأحداث (مثل النقر المزدوج والنقر مع الاستمرار).
3. **الفشل الآمن والاستعادة (Fail-Safe & Restoration):** يجب تخزين موضع القراءة (Surah, Ayah) بشكل آمن واسترجاعه فور فتح التطبيق. يُمنع تهيئة Pager/LazyList يعتمد على الجلسة المحفوظة قبل التأكد من تحميل البيانات `isNotEmpty()`.
4. **تكامل الـ API:** لا يُسمح بتجاهل الردود (Silent Failures)؛ يجب أن يكون هناك Timeout سريع (Fail-Fast) وإشعار للمستخدم بانقطاع الاتصال.

## 📈 الوضع الحالي للمشروع
- تم الانتهاء من **المرحلة الثانية (Phase 2)**.
- ميزة استئناف القراءة المحفوظة تعمل بكفاءة.
- مشغل الصوت `ExoPlayer` متوافق بشكل كامل مع إشعارات النظام والخلفية وإدارة `AudioFocus`.
- قارئ الشاشة TalkBack يقدم إرشادات منطوقة بوضوح عبر تفكيك النصوص العربية العشوائية واستبدالها بنصوص مبسطة (مثال: "الآية رقم 1").
- **تم اعتماد جميع مراجعات الجودة والأداء والتشفير (Hooks 5, 6, 7).**
