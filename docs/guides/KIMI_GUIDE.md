<div dir="rtl">

# دليل استخدام وتشغيل Claude Code بذكاء Kimi / Moonshot AI (`KIMI_GUIDE.md`)

يوضح هذا المستند كيفية استخدام السكريبت [kimi.ps1](file:///f:/AI%20PROJECTS/Blind%20App/kimi.ps1) لتشغيل أدوات **Claude Code CLI** عبر خوادم ونماذج **Moonshot AI (Kimi)**، مع توضيح توزيع النماذج حسب المهام وطرق التشغيل المختلفة.

---

## 📌 المتطلبات الأساسية (Prerequisites)

1. **مفتاح Moonshot API Key:**
   - ضع مفتاح الـ API داخل ملف باسم `.kimi-key` في المجلد الرئيسي للمشروع (`f:\AI PROJECTS\Blind App\.kimi-key`).
   - أو قُم بتعيينه كمتغير بيئة للنظام: `$env:MOONSHOT_API_KEY = "your_key_here"`.

2. **Claude Code CLI:**
   - أن تكون أداة `claude` مثبتة على الجهاز ومتاحة في مسار الأوامر (PATH).

---

## 🎯 توزيع النماذج التلقائي حسب مستوى المهمة (Task Tier Mapping)

يتحكم السكريبت تلقائياً في توجيه طلبات Claude Code إلى نماذج Kimi المناسبة بناءً على نوع المهمة المعالجة:

| مستوى المهمة (Tier) | نموذج Kimi المخصص | الغرض التقني |
| :--- | :--- | :--- |
| **Opus (التخطيط والمعمارية)** | `kimi-k3` | المهام التفكيرية المعقدة، تصميم المعمارية البرمجية، وحل الألغاز التقنية. |
| **Sonnet (التطوير اليومي)** | `kimi-k3` | كتابة الكود البرمجي، تطوير الميزات، وإعادة الهيكلة (Refactoring). |
| **Haiku (المهام السريعة والتجميع)** | `kimi-k2.7-code-highspeed` | استدعاء الأوامر السريعة، الوكلاء الفرعيين، واستفسارات الباش والخلفية. |

---

## 🚀 طرق وأوامر التشغيل (Usage Commands)

### 1. التشغيل الافتراضي الموزّع (الموصى به)
يقوم بتوزيع المهام تلقائياً (`kimi-k3` للمهام الأساسية و `kimi-k2.7-code-highspeed` للمهام السريعة):
```powershell
.\kimi.ps1
```

### 2. التشغيل المباشر مع سؤال أو برومبت:
```powershell
.\kimi.ps1 "اشرح لي بنية هذا المشروع وكيف تعمل الشاشات"
```

### 3. وضع السرعة العالية لكافة المهام (`-Fast` Mode):
يحول كافة المستويات إلى نموذج `kimi-k2.7-code-highspeed` لسرعة الاستجابة وتوفير التكلفة:
```powershell
.\kimi.ps1 -Fast
```

### 4. وضع التفكير والتصميم العميق (`-Opus` Mode):
يحول كافة المستويات إلى النموذج القوي `kimi-k3` للتحليل العميق:
```powershell
.\kimi.ps1 -Opus
```

### 5. تحديد نموذج مخصص يدوياً (`-Model` Parameter):
```powershell
.\kimi.ps1 -Model "kimi-k3"
```

---

## 🛠️ حل المشاكل الشائعة (Troubleshooting)

### 1. خطأ قيود صلاحيات التشغيل (Execution Policy Restriction)
إذا واجهتك خطأ في عدم السماح بتشغيل السكريبتات في PowerShell:
```powershell
powershell -ExecutionPolicy Bypass -File .\kimi.ps1
```

### 2. تغيير النموذج أثناء الجلسة المفتوحة (Inside Claude CLI)
يمكنك التحقق من النموذج الحالي أو تغييره داخل جلسة Claude Code عبر كتابة:
```text
/model
```

---

*تم التوثيق بواسطة طاقم الوكلاء المساعدين لـ Blind App.*

</div>
