# HANDOFF — Cowork session → Claude Code CLI (2026-09-11)

Read `CLAUDE.md` and `fleet_orders/CURRENT_STATE.md` first. This file overrides §1 of CURRENT_STATE
where they disagree. Reply to Ibrahim in Arabic (technical terms in English), per CLAUDE.md.

## Why you are taking over
The Cowork session cannot run a shell on this machine (B-10: a Windows update from 2026-09-08
breaks the Cowork VM's Plan9 mount). Claude Code CLI is unaffected, so git, Gradle and tests are yours.

## Facts established this session (verify, do not trust)
1. `shared/.gitignore` (`/build`) was added — B-16. Root `.gitignore` has `/build` (root-anchored),
   `app/` has its own `.gitignore`, `shared/` had none, so `shared/build/**` would have been committed.
2. Ibrahim then ran the `git add -A app/src shared iosApp …` from CURRENT_STATE §1. `git status --short`
   afterwards showed ONLY `A shared/.gitignore` staged — no `app/src`, `shared/build.gradle.kts`,
   `iosApp`, root Gradle files, and no `D` entries for the 4 deleted voice files.
   => Either P0-001 / P0-006 were **already committed earlier**, or those paths are **ignored/untracked**.
   CURRENT_STATE's "nothing is committed yet" is therefore stale or wrong.
3. Commit **2ee1a62** on branch `feat/ios-kmp-phase0` contains only `shared/.gitignore`
   (1 file, 1 insertion) but carries a **misleading message** ("add :shared module skeleton and remove
   voice commands"). It must be corrected.
4. Unrelated working-tree changes — **do not stage or touch**: `.agents/HOOKS_GUIDE.xlsx` (modified),
   `remote_ios_dev_playbook_diagram.html` (untracked, 736 KB; Ibrahim decides).

## Tasks, in order — stop and report on any surprise
1. **Establish git truth.** Run and show verbatim:
   `git status -sb`, `git log --oneline -10`, `git show --stat HEAD`,
   `git ls-files shared iosApp build.gradle.kts settings.gradle.kts gradle/libs.versions.toml`,
   `git log --oneline -3 -- shared/build.gradle.kts app/src/main/java/com/example/ui/screens/QuranPlayerScreen.kt`.
   If P0-001/P0-006 files are NOT tracked, run `git check-ignore -v` on them, report the rule, and STOP.
2. **Fix 2ee1a62's message** — only if it is not on any remote (`git branch -r --contains HEAD` is empty).
   `git commit --amend` with:
   `chore(shared): add shared/.gitignore to keep shared/build out of VCS (B-16)`
   plus the attribution trailers from CLAUDE.md / session. If it IS on a remote: do not amend,
   do not force-push — report and ask Ibrahim.
3. **Quality Gate (Testing Monopoly — you run it).** `.\gradlew :app:assembleDebug` and
   `.\gradlew :app:testDebugUnitTest`. Paste the result tail verbatim. Confirm
   `git grep -nE "VoiceCommand|SpeechRecognizer|RECORD_AUDIO|RecognitionService" -- app/src` is empty.
4. **Settle B-06.** Read `app/src/main/java/com/example/data/local/SessionPreferences.kt` and
   `git grep -nE "EncryptedSharedPreferences|MasterKey|security\.crypto" -- app/src`.
   State plainly whether `androidx.security.crypto` (`app/build.gradle.kts:114`) is used.
   Do NOT remove the dependency yourself — Gradle edits belong to OpenCode; write the finding only.
5. **Update `fleet_orders/CURRENT_STATE.md`**: §1 (real commit state), §5 (P0-005 unblocked or not),
   §6 (B-06 result, B-10 cause confirmed by the app itself: 2026-09-08 Windows update). Verify the file
   content after writing. Commit it together with this HANDOFF file as
   `docs(fleet): update CURRENT_STATE after B-16 and B-06 checks`.
6. **Do not push.** Report to Ibrahim in Arabic: what you found, gate result, B-06 verdict.
   Next open decision remains **ADR-004** (CURRENT_STATE §4) — present it, do not decide it.
