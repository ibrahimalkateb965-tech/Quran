# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx4g -XX:+UseG1GC -XX:MaxMetaspaceSize=768m -Dfile.encoding=UTF-8
# Kotlin compiler runs in its own daemon (see strategy below); give it its own heap.
kotlin.daemon.jvmargs=-Xmx3g -XX:MaxMetaspaceSize=512m
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. For more details, visit
# https://developer.android.com/r/tools/gradle-multi-project-decoupled-projects
org.gradle.parallel=false
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official
# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true
org.gradle.caching=true
org.gradle.configuration-cache=true
# Set the maximum number of workers to 2 to avoid overloading memory.
org.gradle.workers.max=2
# ORDER-P0-001: in-process ran the Kotlin compiler inside the 2 GB Gradle daemon.
# That is what produced hs_err_pid14964.log / hs_err_pid22496.log (OOM, 16 GB box).
# Kotlin/Native linking needs far more, so the compiler gets its own daemon process.
kotlin.compiler.execution.strategy=daemon

# Windows host cannot build Kotlin/Native iOS targets. This keeps Gradle CONFIGURATION
# succeeding locally; iOS compilation happens only on the macos-15 CI runner.
kotlin.native.ignoreDisabledTargets=true
# Allow the google-services plugin to work even if the google-services.json is not present.
googleServices.missing.passthrough=true
