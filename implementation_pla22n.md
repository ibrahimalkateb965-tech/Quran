<div dir="rtl">

# خطة الإصلاح الجذري لانهيار (Extension Host) والـ Terminal

تهدف هذه الخطة إلى حل مشكلة انهيار المحرر وتوقف الـ Terminal المدمج عن الاستجابة من جذورها، لضمان بيئة عمل مستقرة بنسبة 100%.

## ⚠️ مراجعة المستخدم المطلوبة (User Review Required)

> [!CAUTION]
> تنفيذ هذه الخطة سيؤدي إلى إعادة تعيين بعض إعدادات الـ Terminal الداخلية ومسح ذاكرة التخزين المؤقت للإضافات (Cache) لإزالة الملفات التالفة. لن يؤثر ذلك على ملفاتك البرمجية أو مشروعك، ولكن قد يتطلب منك تسجيل الدخول لبعض الإضافات مجدداً.

## ❓ أسئلة مفتوحة (Open Questions)

> [!WARNING]
> 1. هل ظهرت هذه المشكلة (توقف الـ Terminal) فقط بعد تثبيت إضافة معينة (مثل Claude Code) أم أنها مشكلة قديمة؟
> 2. هل تستخدم إصدار PowerShell 7 أم الإصدار الافتراضي (Windows PowerShell 5.1)؟

## 🛠️ التغييرات والإجراءات المقترحة (Proposed Changes)

سيتم تنفيذ خطة الإصلاح على 3 مراحل رئيسية:

### المرحلة الأولى: تنظيف الذاكرة المؤقتة للمحرر (Cache Wipe)
- مسح مجلدات التخزين المؤقت الخاصة بـ Extension Host والتي غالباً ما تحتوي على بيانات تالفة (Corrupted IPC Data) تسبب حلقة الانهيار المستمرة (Crash Loop).
- إزالة الإضافات المعلقة أو التي فشل تثبيتها بشكل سليم.

### المرحلة الثانية: إعادة هيكلة إعدادات الـ Terminal المدمج
- سنقوم بتعديل ملف الإعدادات الرئيسي للمحرر (`settings.json`) الخاص ببيئة المستخدم.
- سنضيف إعدادات صارمة لفرض تخصيص (Terminal Profile) سليم ومستقل لا يتأثر بانهيار الإضافات الأخرى، مع تفعيل بروتوكول GPU Acceleration لتسريع استجابته.

### المرحلة الثالثة: الفحص الآلي للإضافات (Extension Audit)
- تنفيذ فحص عبر سطر الأوامر للإضافات المثبتة.
- تعطيل/إزالة الإضافة المسببة للمشكلة (والتي ظهرت في الصور مسبقاً) بشكل نظيف من الجذور لتفادي ترك أي بقايا (Leftovers).

---

## 🔍 خطة التحقق (Verification Plan)

بعد التنفيذ، سنتحقق من استقرار النظام عبر:

### التحقق الآلي (Automated Tests)
- إطلاق جلسة Terminal داخلية جديدة والتحقق من الاستجابة السريعة (Response Time).
- قراءة ملفات السجل (Logs) الخاصة بالـ Extension Host للتأكد من اختفاء الخطأ: `Extension host terminated unexpectedly`.

### التحقق اليدوي (Manual Verification)
- سنطلب منك فتح الـ Terminal داخل المحرر وتجربة كتابة أي أمر، والتأكد من عدم ظهور رسالة الانهيار الحمراء مجدداً خلال 5 دقائق من الاستخدام المكثف.

</div>
